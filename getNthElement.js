/*Dado un arreglo y un entero, getNthElement devuelve el valor según
 el entero dado, dentro del arreglo dado.

Notas:
Si el arreglo tiene una longitud de 0, debería devolver' undefined'.
*/

const getNthElement = ((array, int)=>{
    const index= array[int];
    return index;
});